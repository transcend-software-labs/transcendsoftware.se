<section id="hero" class="hero section">
  <div class="container container-wide">
    <h1 class="hero-name">Rasmus Kockum</h1>
    <p class="hero-title">Senior Software Engineer &amp; Tech Lead</p>
    <p class="hero-subtitle">
      I build backend systems, ship CI/CD pipelines, and help teams move faster with AI.
      Mostly in Java, Go, and TypeScript.
    </p>
    <a href="#services" class="hero-cta mono">
      What I do &darr;
    </a>
  </div>
</section>

<style>
  .hero {
    padding-top: 10rem;
    padding-bottom: 8rem;
  }

  .hero-name {
    font-family: var(--font-mono);
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 600;
    letter-spacing: -0.03em;
    color: var(--text);
    margin-bottom: 0.5rem;
  }

  .hero-title {
    font-family: var(--font-mono);
    font-size: clamp(0.95rem, 2vw, 1.1rem);
    color: var(--text-muted);
    margin-bottom: 1.5rem;
  }

  .hero-subtitle {
    font-size: 1.15rem;
    line-height: 1.6;
    color: var(--text-muted);
    max-width: 52ch;
    margin-bottom: 3rem;
  }

  .hero-cta {
    display: inline-block;
    font-size: 0.85rem;
    color: var(--text-dim);
    padding-bottom: 0.25rem;
    border-bottom: 1px solid var(--border);
    transition: color 0.15s ease, border-color 0.15s ease;
  }

  .hero-cta:hover {
    color: var(--accent);
    border-color: var(--accent);
  }
</style>
